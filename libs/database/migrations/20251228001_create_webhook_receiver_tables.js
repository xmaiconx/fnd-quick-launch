/**
 * Migration: Create Webhook Receiver Tables (Stub)
 * This is a stub file to resolve migration directory corruption
 *
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  // Already applied - this is a stub
  return Promise.resolve();
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
  // Already applied - this is a stub
  return Promise.resolve();
};
